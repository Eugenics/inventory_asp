@model inventory_dot_core.Models.RelHardwareEmployee

@{
    ViewData["Title"] = $"Добавление оборудавания на РМ сотрудника: {ViewBag.EmployeeName}";
}

<!-- Modal placeholder -->
<div id="modal-placeholder"></div>

<h1>Добавление оборудования</h1>

<h4>На РМ сотрудника: @ViewBag.EmployeeName</h4>
<hr />
<!--
<a id="modal_btn"
   class="btn btn-primary"
   data-toggle="ajax-modal"
   data-target="#add-contact"
   data-url="@Url.Action("Contact")">
    Add contact
</a>
    -->


<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!--
            <div class="input-group mb-3">
                <input name="hardwareSearch" id="hardwareSearch" class="form-control mr-sm-2" placeholder="Поиск" aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-outline-success my-2 my-sm-0">Поиск</button>
                </div>
            </div>
                -->
            <div class="form-group">
                <label asp-for="RelheWhardId" class="control-label"></label>
                <select asp-for="RelheWhardId" class="form-control" asp-items="ViewBag.RelheWhardId"></select>
            </div>
            <div class="form-group">
                <input type="submit"
                       value="Сохранить"
                       class="btn btn-outline-primary"
                       method="post"
                       asp-route-employee_id="@ViewBag.EmployeeId"
                       asp-route-filter="@ViewBag.Filter"
                       asp-route-sortExpression="@ViewBag.SortExpression"
                       asp-route-page="@ViewBag.Page" /> |
                <a class="btn btn-outline-primary" asp-action="Index"
                   asp-route-employee_id="@ViewBag.EmployeeId"
                   asp-route-filter="@ViewBag.Filter"
                   asp-route-sortExpression="@ViewBag.SortExpression"
                   asp-route-page="@ViewBag.Page">
                    Назад
                </a>
            </div>
        </form>
    </div>
</div>

<script src="~/js/scripts/emp_workplace.js"></script>

@section Scripts {
    <!--
    <script src="~/lib/jquery/dist/jquery-3.4.1.js"></script>
        <script src="~/mdb/js/bootstrap.js"></script>
        <script src="~/js/jquery-ui.js"></script>
        <script src="~/js/scripts/modal.js"></script>
        <script>
            $(document).ready(function () {
                $("#hardwareSearch").autocomplete({ source: window.location.origin + "/api/searchapi/search" });
            })
        </script>
        -->
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
